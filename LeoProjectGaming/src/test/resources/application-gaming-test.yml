spring:
  datasource:
    url: jdbc:h2:mem:testgamingdb
    driverClassName: org.h2.Driver
    username: sa
    password: as
  h2:
    console:
      enabled: true
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
